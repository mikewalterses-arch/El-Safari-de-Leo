// TODO(fase-2): flujo de nuevo avistamiento (foto + GPS + búsqueda en catálogo + nota)
export function NewSighting() {
  return (
    <div>
      <h2 className="text-2xl font-extrabold">Nuevo avistamiento</h2>
      <p className="mt-2">Captura + identificación manual — pendiente fase 2.</p>
    </div>
  );
}
